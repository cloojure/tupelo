var globalObject = {};
globalObject.a = 0;  // value doesn't matter
globalObject.b = 0;
globalObject.c = 0;

var makeDino = function() {};  // fn body doesn't matter
makeDino.desc = 0;  // type doesn't matter
makeDino.says = function(something) {};  // # of args must be correct; names don't matter
